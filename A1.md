# Câu A1: 3 Cách nhúng CSS

Có 3 cách để nhúng CSS vào HTML là: **Inline CSS**, **Internal CSS** và **External CSS**. Dưới đây là chi tiết cho từng cách.

---

## 1. Inline CSS (CSS nội tuyến)
Nhúng trực tiếp mã CSS vào thẻ HTML thông qua thuộc tính `style`.

- **Ví dụ code:**
```html
<p style="color: blue; font-size: 16px;">Đoạn văn bản này sử dụng Inline CSS.</p>
```

- **Ưu điểm:**
  - Áp dụng nhanh chóng và trực tiếp cho một phần tử duy nhất.
  - Độ ưu tiên (specificity) rất cao, dễ dàng ghi đè các quy tắc CSS khác.
  - Không cần tạo thêm file hoặc thêm mã vào phần `<head>`.

- **Nhược điểm:**
  - Rất khó bảo trì: Nếu muốn đổi màu cho 100 đoạn văn, bạn phải sửa 100 chỗ.
  - Làm mã HTML trở nên cồng kềnh, lộn xộn và khó đọc.
  - Mất đi tính tái sử dụng (reusability) của CSS.

- **Khi nào nên dùng:**
  - Khi cần kiểm thử (test) nhanh một giao diện.
  - Khi áp dụng một style độc nhất vô nhị cho một thẻ mà chắc chắn không tái sử dụng ở đâu khác.
  - Khi sử dụng JavaScript để thay đổi style động (JavaScript thường thay đổi CSS thông qua inline style).

---

## 2. Internal CSS (CSS cục bộ)
Nhúng mã CSS vào bên trong cặp thẻ `<style>`, thường được đặt ở phần `<head>` của file HTML.

- **Ví dụ code:**
```html
<!DOCTYPE html>
<html>
<head>
  <style>
    p {
      color: red;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <p>Đoạn văn bản này sử dụng Internal CSS.</p>
</body>
</html>
```

- **Ưu điểm:**
  - Gom toàn bộ CSS của một trang vào một khu vực duy nhất (`<head>`).
  - Dễ dàng định kiểu cho toàn bộ một file HTML mà không ảnh hưởng đến các file HTML khác.
  - Không cần tạo và quản lý thêm file file CSS riêng biệt.

- **Nhược điểm:**
  - Không thể chia sẻ hoặc tái sử dụng CSS cho các trang (file HTML) khác trong cùng website.
  - Làm tăng kích thước file HTML, khiến trang web load chậm hơn một chút nếu đoạn CSS quá dài.

- **Khi nào nên dùng:**
  - Khi xây dựng các trang web đơn lẻ (single-page website).
  - Khi một trang web cần có một giao diện hoàn toàn khác biệt so với phần còn lại của website.
  - Khi làm các mẫu Email HTML (HTML Email templates) vì nhiều email client không hỗ trợ hoặc giới hạn External CSS.

---

## 3. External CSS (CSS ngoại tuyến)
Viết CSS vào một file riêng biệt có đuôi `.css` và nhúng vào file HTML thông qua thẻ `<link>`.

- **Ví dụ code:**
*Trong file `style.css`:*
```css
p {
  color: green;
  font-size: 20px;
}
```

*Trong file `index.html`:*
```html
<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <p>Đoạn văn bản này sử dụng External CSS.</p>
</body>
</html>
```

- **Ưu điểm:**
  - Tách biệt hoàn toàn phần cấu trúc (HTML) và phần trình bày (CSS), giúp mã nguồn gọn gàng.
  - **Tính tái sử dụng tối đa**: Một file CSS có thể được nhúng vào hàng trăm trang HTML khác nhau.
  - **Dễ dàng bảo trì**: Chỉ cần thay đổi code ở 1 file CSS, toàn bộ website sẽ được cập nhật.
  - **Tối ưu tốc độ tải trang**: Trình duyệt có thể lưu bộ nhớ đệm (cache) file CSS, giúp các trang load sau này nhanh hơn.

- **Nhược điểm:**
  - Tốn thêm yêu cầu (HTTP request) để tải file CSS, có thể làm chậm thời gian hiển thị trang ở lần truy cập đầu tiên nếu file CSS quá nặng.

- **Khi nào nên dùng:**
  - Trong thực tế, **đây là cách được khuyến khích sử dụng nhất** cho mọi dự án website có từ 2 trang trở lên để đảm bảo tính chuyên nghiệp và dễ quản lý.

---

## Câu hỏi thêm

**Câu hỏi:** Nếu cùng 1 element có cả 3 cách CSS đồng thời áp dụng, cách nào "thắng"? Giải thích tại sao.

**Trả lời:** Cách **Inline CSS** sẽ "thắng" (được ưu tiên áp dụng).

**Giải thích:**
Trình duyệt áp dụng các kiểu CSS dựa trên nguyên tắc **Thác đổ (Cascade)** và **Độ ưu tiên (Specificity)**. Theo quy tắc mặc định, độ ưu tiên từ cao xuống thấp như sau:

1. **Inline CSS** (mức độ đặc hiệu cao nhất vì nhắm thẳng trực tiếp vào một phần tử cụ thể bằng thuộc tính `style`).
2. **Internal CSS** và **External CSS** (được xét ngang hàng nhau). Giữa 2 cách này, cách nào được khai báo **sau cùng** (nằm ở vị trí thấp hơn trong thẻ `<head>`) sẽ "thắng" cái khai báo trước nó.
3. Các style mặc định của trình duyệt.

Vì vậy, do Inline CSS gắn trực tiếp vào phần tử nên nó luôn có độ ưu tiên cao nhất (bỏ qua trường hợp sử dụng từ khóa `!important`).
