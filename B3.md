# Bài B3: Specificity Battle

Dưới đây là phần trả lời cho trận chiến độ ưu tiên CSS (Specificity):

### 1. Liệt kê 10 rules + specificity score
Em đã code 10 CSS rules cùng nhắm vào thẻ `<p id="demo" class="text highlight">` và xếp từ điểm thấp đến cao như sau:

1. `* { color: gray; }` -> Specificity: **(0, 0, 0)**
2. `p { color: pink; }` -> Specificity: **(0, 0, 1)**
3. `.text { color: orange; }` -> Specificity: **(0, 1, 0)**
4. `p.text { color: yellow; }` -> Specificity: **(0, 1, 1)**
5. `.text.highlight { color: green; }` -> Specificity: **(0, 2, 0)**
6. `p.text.highlight { color: teal; }` -> Specificity: **(0, 2, 1)**
7. `#demo { color: blue; }` -> Specificity: **(1, 0, 0)**
8. `p#demo { color: purple; }` -> Specificity: **(1, 0, 1)**
9. `#demo.text { color: brown; }` -> Specificity: **(1, 1, 0)**
10. `p#demo.text.highlight { color: red; }` -> Specificity: **(1, 2, 1)**

### 2. Element cuối cùng hiển thị màu gì? Tại sao?
- **Trả lời:** Chữ "Hello World" sẽ hiển thị màu **đỏ (red)**.
- **Tại sao?** Vì quy tắc số 10 (`p#demo.text.highlight`) có điểm specificity cao nhất là `(1, 2, 1)`. Trình duyệt luôn luôn ưu tiên lấy CSS của rule có điểm số to nhất, nên cái màu đỏ đã đè bẹp tất cả 9 màu của các rule yếu hơn kia.

### 3. Chụp screenshot kết quả
![Screenshot B3](./screenshot_B3.png)

### 4. Thay đổi thứ tự rules trong CSS file. Kết quả có đổi không? Giải thích.
- **Trả lời:** Kết quả **KHÔNG** hề thay đổi, chữ vẫn cứ là màu đỏ.
- **Giải thích:** Bởi vì điểm specificity là yếu tố quyết định "sống còn" mang tính chất cốt lõi. Chỉ khi nào có hai rule **bằng điểm nhau y xì đúc** thì trình duyệt mới xét đến luật "Cascading" (tức là thứ tự xuất hiện từ trên xuống dưới, thằng nào viết sau thì đè thằng trước). Còn ở bài này, rule số 10 có điểm `(1, 2, 1)` là độc tôn vô địch rồi, nên em có bứng nó ném lên đầu hay vứt vào giữa file `specificity.css` thì nó vẫn đứng top 1 server và giữ nguyên màu đỏ!
