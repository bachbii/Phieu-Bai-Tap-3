# Câu A4: Specificity (Độ ưu tiên)

Dưới đây là phần bài làm của mình về tính điểm ưu tiên trong CSS:

### 1. Tính specificity score (a, b, c) cho mỗi rule:
Cách tính là mình đếm số lượng: (ID, Class/Attribute/Pseudo-class, Element/Pseudo-element).
- **Rule A (`p`):** Chỉ có 1 thẻ HTML thôi -> Điểm là **(0, 0, 1)**
- **Rule B (`.price`):** Có 1 class -> Điểm là **(0, 1, 0)**
- **Rule C (`#main-price`):** Có 1 ID -> Điểm là **(1, 0, 0)**
- **Rule D (`p.price`):** Vừa có 1 thẻ HTML vừa có 1 class -> Điểm là **(0, 1, 1)**

### 2. Element sẽ có màu gì? Giải thích:
- **Trả lời:** Element sẽ có màu **đỏ (red)**.
- **Giải thích:** Khi đem điểm số ở câu 1 ra so sánh, dễ thấy Rule C có điểm to nhất (1, 0, 0). Trong CSS thì ID selector có trọng số mạnh hơn hẳn class và thẻ HTML. Do đó, Rule C "ăn" và quyết định thẻ `<p>` có màu đỏ.

### 3. Nếu thêm `style="color: orange;"` thẳng vào thẻ:
- **Trả lời:** Element lúc này sẽ đổi sang màu **cam (orange)**.
- **Giải thích:** Viết style thẳng vào trong HTML như vầy gọi là Inline CSS. Mà luật là Inline CSS thì auto mạnh hơn tất cả các kiểu dùng selector thông thường (kể cả ID hay class có lồng nhau dài cỡ nào cũng thua). Nên màu cam sẽ đè bẹp màu đỏ.

### 4. Nếu Rule A thêm `!important` thì sao?
- **Trả lời:** Element sẽ có màu **đen (black)**.
- **Tại sao?** Vì từ khóa `!important` là trùm cuối trong CSS rồi. Dù Rule A ban đầu có cái điểm specificity quèn nhất đám (0, 0, 1), nhưng một khi đã kẹp thêm `!important` vào thì nó sẽ vô hiệu hóa sạch sành sanh mọi điểm số hay cả Inline CSS. Trình duyệt bắt buộc phải tô màu đen cho thẻ.
