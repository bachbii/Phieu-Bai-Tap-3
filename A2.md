# Câu A2: CSS Selectors — Dự đoán kết quả

1. `h1`
   - **Chọn:** Thẻ `<h1>` nằm bên trong `<header>`.
   - **Text content:** `ShopTLU`

2. `.price`
   - **Chọn:** Tất cả các thẻ có class là `price` (có 2 thẻ `<p>` thỏa mãn).
   - **Text content:** `25.990.000đ` và `45.990.000đ`

3. `#app header`
   - **Chọn:** Thẻ `<header>` nằm bên trong phần tử có `id="app"`.
   - **Text content:** Toàn bộ text bên trong thẻ header, bao gồm khoảng trắng: `ShopTLU Home Products About`

4. `nav a:first-child`
   - **Chọn:** Thẻ `<a>` đầu tiên nằm bên trong thẻ `<nav>`.
   - **Text content:** `Home`

5. `.product.featured h2`
   - **Chọn:** Thẻ `<h2>` nằm bên trong phần tử có đồng thời cả 2 class là `product` và `featured` (là thẻ `<article>` thứ 2).
   - **Text content:** `MacBook Pro`

6. `article > p`
   - **Chọn:** Tất cả các thẻ `<p>` là **con trực tiếp** của thẻ `<article>`. Do có 2 thẻ `<article>`, mỗi thẻ chứa 2 thẻ `<p>` con trực tiếp, nên tổng cộng có 4 element được chọn.
   - **Text content:** `25.990.000đ`, `Mô tả sản phẩm...`, `45.990.000đ` và `Mô tả sản phẩm...`

7. `a[href="/"]`
   - **Chọn:** Thẻ `<a>` có thuộc tính `href` với giá trị chính xác là `/`.
   - **Text content:** `Home`

8. `.top-bar.dark h1`
   - **Chọn:** Thẻ `<h1>` nằm bên trong phần tử có đồng thời cả 2 class `top-bar` và `dark` (chính là thẻ `<header>`).
   - **Text content:** `ShopTLU`
