# Câu A3: Tính toán kích thước Box Model

Bài này tính toán kích thước các khối. Dưới đây là kết quả mình tính cho các trường hợp:

### Trường hợp 1: content-box (mặc định)
- **Chiều rộng hiển thị (Visible width):** Tính tổng chiều rộng content + padding 2 bên + border 2 bên. 
  -> Bằng: 400 + (20 * 2) + (5 * 2) = **450px**.
- **Không gian chiếm trên trang (Total width):** Lấy chiều rộng hiển thị cộng thêm phần margin 2 bên.
  -> Bằng: 450 + (10 * 2) = **470px**.

### Trường hợp 2: border-box
Vì dùng `box-sizing: border-box` nên thuộc tính `width: 400px` lúc này đã bao gồm luôn cả padding và border ở trong đó rồi.
- **Chiều rộng hiển thị:** Chắc chắn là **400px** luôn (bằng đúng cái width khai báo).
- **Kích thước content thực tế:** Do width tổng là 400px, giờ phải trừ ngược đi padding và border 2 bên để ra phần lõi content thực sự bên trong chứa chữ.
  -> Bằng: 400 - (20 * 2) - (5 * 2) = **350px**.
- **Không gian chiếm trên trang:** Lấy chiều rộng hiển thị (400px) cộng thêm phần margin 2 bên là xong.
  -> Bằng: 400 + (10 * 2) = **420px**.

### Trường hợp 3: Margin collapse
- **Khoảng cách giữa box-a và box-b:** Sẽ là **40px**.
- **Tại sao lại không phải 65px?** 
  Bình thường nghe có vẻ là lấy 25 + 40 = 65, nhưng theo luật "Margin collapse" (sập/gộp margin) của CSS đối với các thẻ block nằm dọc liền kề nhau, trình duyệt sẽ không cộng dồn hai cái margin này lại. Thay vào đó, nó sẽ đem so sánh hai giá trị và "chốt" lấy cái lớn hơn. Ở đây 40 lớn hơn 25 nên khoảng cách thực tế bị đẩy ra chỉ là 40px thôi.

### Câu hỏi Nâng cao:
Nếu `.box-a` có `margin-bottom: -10px` và `.box-b` có `margin-top: 40px`, thì khoảng cách lúc này là bao nhiêu?
- **Trả lời:** Khoảng cách là **30px**.
- **Lý do:** Đối với trường hợp có 1 cái margin dương và 1 cái margin âm gộp với nhau, quy tắc của trình duyệt là lấy hai số cộng thẳng vào nhau (tức là tính tổng đại số). Nên tính ra sẽ là: 40 + (-10) = 30px.
