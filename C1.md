# Bài C1: Debug CSS Layout

Chào thầy/cô, dưới đây là phần phân tích lỗi và đề xuất cách fix layout của em:

### 1. Tính chiều rộng thực tế (content-box)
Do trình duyệt luôn cài đặt mặc định là `box-sizing: content-box`, nên chiều rộng thực tế của một cái hộp bị đội lên khi nó "cõng" thêm padding và border.
- **Chiều rộng thực tế của Sidebar:** `300px` (width) + `40px` (padding trái phải) + `2px` (border trái phải) = **342px**.
- **Chiều rộng thực tế của Content:** `660px` (width) + `60px` (padding trái phải) + `2px` (border trái phải) = **722px**.

### 2. Giải thích tại sao layout bị vỡ
Để cái sidebar và content có thể nắm tay nhau đứng chung một dòng, thì tụi nó cần một không gian có tổng chiều rộng là: `342 + 722 = 1064px`.
Tuy nhiên, thẻ `.container` bọc bên ngoài lại bị khóa cứng ở mức `width: 960px`. Rõ ràng là `1064px > 960px` (bị lòi ra ngoài 104px), không đủ đất diễn nên khối `.content` bị vấp và rớt luôn xuống dòng dưới.

### 3. Hai cách sửa (Fix)

**Cách 1: Dùng `border-box` (Cách khôn ngoan nhất)**
Em chỉ việc thêm dòng lệnh `box-sizing: border-box;` vào CSS của thẻ `.sidebar` và `.content`.
Lúc này, padding và border bị ép "chui tọt" vào bên trong cái `width`. Kích thước của hai khối giữ nguyên cứng ngắc ở mức 300px và 660px. Tổng vừa y xì đúc 960px, khít rịt trong cái container!

**Cách 2: Không dùng `border-box` (Cách tính trừ hao thủ công)**
Nếu vẫn cứ thích để `content-box` mặc định, em bắt buộc phải tự trừ hao phần bị lố ở cái thuộc tính `width`:
- **Ở thẻ Sidebar:** Lấy 300px ban đầu đem trừ đi 42px (padding + border) = Sửa lại code thành `width: 258px`.
- **Ở thẻ Content:** Lấy 660px ban đầu đem trừ đi 62px (padding + border) = Sửa lại code thành `width: 598px`.
Cộng hai cái chiều rộng phình ra (258+42 và 598+62) lại thì vẫn vừa vặn 960px, layout lại đẹp như chưa từng có cuộc chia ly!

---
*Ghi chú: Thầy cô nhớ mở cái file `debug_layout.html` em nộp kèm lên để xem demo trực quan bằng mắt nhé. Em làm cả 2 layout nằm trên dưới cho thầy cô dễ so sánh, cả 2 cách em đều code cho nó hết bị rớt dòng rồi đấy ạ!*
