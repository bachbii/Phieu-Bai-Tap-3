# Bài B1: Style trang Profile

Chào thầy/cô, đây là phần trả lời cho câu hỏi lý thuyết bài B1 của em. 

Em đã tạo một bản copy của file `profile.html` (từ bài cũ) sang thư mục này, nhúng file `style.css` vào và chỉnh trang lại cho đẹp đúng như các yêu cầu đề bài. Em có cố tình gắn thêm cái `class="active"` vào cái thẻ `<a>` chữ "Về tôi" để code CSS của em chạy được trạng thái active.

Dưới đây là danh sách liệt kê **ít nhất 5 loại selector** khác nhau mà em đã sử dụng thực tế trong file `style.css`:

1. **Universal Selector (Bộ chọn toàn cục):** 
   - Thể hiện: `*`
   - Đoạn code em dùng: `* { box-sizing: border-box; }`
   - Tác dụng: Nhắm vào tất tần tật các thẻ HTML trên trang để setup `box-sizing`, tránh lỗi méo layout lúc em thêm padding.

2. **Element Selector / Type Selector (Bộ chọn thẻ):**
   - Thể hiện: `body`, `header`, `table`, `footer`...
   - Đoạn code em dùng: `body { font-family: Arial... }`
   - Tác dụng: CSS trực tiếp lên các thẻ HTML gốc. Ví dụ chọn body để quy định font chữ với màu nền chung cho cả trang.

3. **Descendant Selector (Bộ chọn con cháu):**
   - Thể hiện: `nav ul`, `nav a`
   - Đoạn code em dùng: `nav a { text-decoration: none; ... }`
   - Tác dụng: Giúp em chỉ chọn mấy thẻ `<a>` nằm lọt thỏm trong thẻ `<nav>` thôi. Nhờ vậy mấy cái link ở phần liên hệ (nằm ngoài nav) sẽ không bị dính cái style này.

4. **Class Selector (Bộ chọn class):**
   - Thể hiện: `.active`
   - Đoạn code em dùng: `nav a.active { color: #ffffff; ... }`
   - Tác dụng: Nhắm đích danh vào cái link hiện tại (đang có class `.active`) để làm nổi bật nó lên cho khác bọt so với các link kế bên.

5. **Pseudo-class Selector (Bộ chọn lớp giả):**
   - Thể hiện: `:hover`, `:nth-child(even)`
   - Đoạn code em dùng: `nav a:hover`, `tbody tr:nth-child(even)`, `tbody tr:hover`
   - Tác dụng: Em dùng cái này để bắt sự kiện lúc user di chuột (hover) làm đổi màu thẻ. Đặc biệt là dùng `:nth-child(even)` để làm hiệu ứng ngựa vằn (zebra) xen kẽ hai màu xanh trắng cho cái bảng nhìn cho xịn.

6. **Bonus thêm ID Selector (Bộ chọn ID):**
   - Thể hiện: `#lien-he`
   - Đoạn code em dùng: `#lien-he { padding: 15px; ... }`
   - Tác dụng: Target thẳng vào đúng cái cục section liên hệ để em cho nó cái đổ bóng sương sương.

---
*Ghi chú: Thầy/cô nhớ mở thử file `profile.html` ở cùng thư mục này lên để xem giao diện CSS em làm nhé ạ!*
